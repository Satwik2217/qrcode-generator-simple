🚀 QR Code Utility MicroserviceThis project is a simple, functional Node.js/Express API that serves as a backend utility for URL management and QR code generation.It demonstrates foundational skills in API design and file persistence without relying on a traditional database.✨ Key FeaturesAPI-Powered QR Generation: Instantly creates a QR code image (Base64-encoded PNG) for pre-defined URLs using the qrcode library.File-Based Persistence: Stores all URL mappings in a local JSON file (./data/urls.json) managed by the Node.js fs module.Browser Preview: The generated QR code is returned within an HTML response for easy viewing in any web browser.🛠️ Tech StackBackend: Node.js, Express.jsPersistence: fs Module (JSON file store)Utility: qrcode🌐 API EndpointThe API base URL is http://localhost:<PORT>.MethodEndpointPurposeGET/urlsRetrieves all stored URL mappings.GET/urls/:keyFetches the URL associated with the key and generates the QR Code image.🏃 Quick RunClone the repo.Install: npm installRun: npm start (or nodemon index.js)Test: Open http://localhost:3000/urls/YOUR_KEY in your browser to see the QR code
